require 'spec_helper'

describe SugarMama do
  it 'has a version number' do
    expect(SugarMama::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
